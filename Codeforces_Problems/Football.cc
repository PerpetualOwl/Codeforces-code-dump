#include <iostream>

int solution(int n, int scored, int conceded) {
    
    return
}

int main() {
    std::string line1;
    std::string line2;
    std::string line3;
    std::getline(std::cin, line1);
    std::getline(std::cin, line2);
    std::getline(std::cin, line3);

    solution(stoi(line1), stoi(line2), stoi(line3));
    return 0;
}